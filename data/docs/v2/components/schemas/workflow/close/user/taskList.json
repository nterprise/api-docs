{
  "type": "object",
  "description": "How to mark task-list step as complete",
  "required": [
    "fields",
    "step"
  ],
  "properties": {
    "items": {
      "type": "array",
      "description": "Configuration for each task list item",
      "items": {
        "type": "object",
        "required": [
          "value",
          "label"
        ],
        "properties": {
          "value": {
            "type": "string",
            "enum": [
              "ok",
              "na",
              "bad"
            ],
            "description": "Selects which item the user has selected",
            "example": "ok",
            "x-examples": [
              "ok"
            ]
          },
          "slug": {
            "type": "string",
            "description": "Slug for the item. Matches the slug in the step definition",
            "readOnly": true,
            "pattern": "^[a-z0-9]+(?:-[a-z0-9]+)*$",
            "example": "asset-tag-placement",
            "x-examples": [
              "asset-tag-placement"
            ]
          }
        }
      }
    }
  },
  "allOf": [
    {
      "$ref": "../../steps/stepCloseCommon.json"
    }
  ]
}

{
  "type": "object",
  "properties": {
    "label": {
      "example": "Is device ok?",
      "x-examples": [
        "Is device ok?"
      ]
    },
    "type": {
      "example": "choice",
      "x-examples": [
        "choice"
      ]
    },
    "choices": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "goto"
        ],
        "maxProperties": 2,
        "minProperties": 2,
        "maximum": 20,
        "properties": {
          "and": {
            "type": "array",
            "description": "All choices must be satisfied",
            "maximum": 5,
            "items": {
              "$ref": "./stepChoice.json"
            }
          },
          "or": {
            "type": "array",
            "description": "Any of these choices must be satisfied",
            "maximum": 5,
            "items": {
              "$ref": "./stepChoice.json"
            }
          },
          "not": {
            "type": "array",
            "description": "None of these choices must be satisfied",
            "maximum": 5,
            "items": {
              "$ref": "./stepChoice.json"
            }
          },
          "goto": {
            "description": "Step to goto when condition is met",
            "allOf": [
              {
                "$ref": "./stepCommon.json#/properties/goto"
              }
            ]
          }
        }
      }
    },
    "goto": {
      "description": "Default step to goto when no choice can be made"
    }
  },
  "allOf": [
    {
      "$ref": "./stepCommon.json"
    }
  ]
}

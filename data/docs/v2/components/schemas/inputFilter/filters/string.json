{
  "description": "Filter to transform a value into a string",
  "type": "object",
  "required": [
    "type",
    "options"
  ],
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "string"
      ]
    },
    "options": {
      "type": "object",
      "description": "String filter has no options",
      "example": {},
      "x-examples": [
        {}
      ]
    }
  }
}

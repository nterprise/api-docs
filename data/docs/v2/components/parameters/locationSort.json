{
  "name": "sort",
  "in": "query",
  "description": "Sort by field",
  "schema": {
    "type": "string",
    "enum": [
      "country",
      "-country",
      "created",
      "-created",
      "updated",
      "-updated"
    ]
  }
}

{
  "name": "filter[administrative_area]",
  "in": "query",
  "description": "Filter where the administrative area (state or province) contains this value",
  "schema": {
    "$ref": "../schemas/location.json#/properties/address/properties/administrative_area"
  }
}

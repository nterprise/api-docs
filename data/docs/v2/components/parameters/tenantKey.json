{
  "name": "tenant_key",
  "in": "path",
  "required": true,
  "description": "The key of the tenant to retrieve",
  "schema": {
    "type": "string",
    "pattern": "/(^[a-z]|[A-Z0-9])[a-z]*/",
    "example": "tenant-key"
  }
}

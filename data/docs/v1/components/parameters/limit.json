{
  "name": "limit",
  "description": "How many items to return at one time (max 100)",
  "in": "query",
  "required": false,
  "schema": {
    "type": "integer",
    "format": "int32",
    "maximum": 100,
    "minimum": 1
  }
}

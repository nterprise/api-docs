{
  "name": "page_size",
  "description": "How many items to return at one time (max 100)",
  "in": "query",
  "required": false,
  "schema": {
    "type": "number",

    "maximum": 100,
    "minimum": 1
  }
}

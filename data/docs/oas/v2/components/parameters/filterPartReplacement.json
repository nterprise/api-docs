{
  "name": "filter[replacement_type]",
  "in": "query",
  "description": "Filter where the replacement type is this value",
  "schema": {
    "type": "string",
    "description": "Describes how a the part can be replaced",
    "default": "replaces",
    "enum": [
      "replaces",
      "substitutes"
    ]
  }
}

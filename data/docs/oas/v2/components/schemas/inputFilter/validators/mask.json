{
  "description": "Validate string matches a regular expression",
  "type": "object",
  "required": [
    "type",
    "options"
  ],
  "properties": {
    "type": {
      "type": "string",
      "enum": [
        "mask"
      ]
    },
    "options": {
      "type": "object",
      "required": [
        "mask"
      ],
      "properties": {
        "mask": {
          "type": "string",
          "description": "Mask to validate against",
          "example": "^[a-z]$",
          "x-examples": [
            "^[a-z]$"
          ]
        }
      }
    }
  }
}
